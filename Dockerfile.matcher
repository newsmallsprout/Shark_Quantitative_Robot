FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY matcher/ ./
RUN go mod tidy && CGO_ENABLED=0 go build -o shark-matcher .

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /app/shark-matcher /usr/local/bin/
CMD ["shark-matcher"]
