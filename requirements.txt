# Shark 2.0 — 默认运行 main.py / FastAPI 所需直接依赖（审计日期：与代码 import 对齐）
# 按需加回的可选包见文件末尾注释。

aiohttp>=3.8.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
redis>=5.0.0

# -----------------------------------------------------------------------------
# 以下曾列入但当前仓库 *.py 未引用；启用对应功能时再取消注释并锁定版本。
#
# certifi>=2023.7.22          # 一般随 aiohttp/urllib 传递安装
# pydantic>=2.0.0            # 由 fastapi 引入；仅当需独立 pin 版本时再加
# ccxt>=4.0.0                # 实盘/ccxt 适配层
# redis>=5.0.0               # 缓存 / 分布式状态
# cryptography>=41.0.0       # license/密钥工具脚本
# loguru>=0.7.0              # 若替换 stdlib logging
# psutil>=5.9.0              # 主机与进程监控
# pyyaml>=6.0.1              # YAML 配置驱动
# jinja2>=3.1.2              # 模板渲染服务
# pyzmq>=25.1.0              # ZeroMQ 消息
